计划：

1. 在 `src/pages/MyMarketing.tsx` 删除「工作流提示带」整段 UI，也就是截图里的 `01 / 02 / 03` 横向流程条。
2. 删除已经不再使用的 `FlowDot` 组件，避免后续又被复用显示出来。
3. 不改动「创作工坊」卡片、BOOMER 帮我拍、素材库和发布账号入口，确保只移除这条步骤条。
4. 完成后检查页面中不再出现「拍图 / 上传」「AI 产出」「复制发布」这组三段流程提示。