排查结果：
- 黑色背景不是页面容器问题，而是动画资源/兜底链路仍可能显示带黑底的头像帧；当前截图里的小头像和大头像都有概率落到黑底资源。
- 排班数据库是有数据的：今天、明天都有鹿哥的 B 班记录；但 `spirit-chat` 只查“今天”，并且班次详情要求 `shop_id` 必须来自 `staff_profiles`。当前鹿哥的 `staff_profiles.shop_id` 为空，所以 AI 上下文被构造成“今天没有排班”，导致回答错误。

实施计划：
1. 小精灵头像稳定透明化
   - 重新生成/替换小精灵动画资源，确保 WebM 与 APNG 每一帧透明，不再带黑色画布。
   - 调整 `SpiritMascot` 的兜底策略：如果视频透明播放不可靠，优先使用透明 APNG/静态 PNG，不再显示任何黑底帧。
   - 检查聊天顶部头像、空状态大头像、消息小头像、悬浮按钮四处都使用同一套稳定透明组件。

2. 修复 AI 排班上下文
   - 修改 `spirit-chat`：同时读取今天和明天的排班，而不是只读今天。
   - 不再只依赖 `staff_profiles.shop_id`；优先从用户自己的 `shift_schedules.shop_id` 推断门店，再回退到 `staff_profiles.shop_id`。
   - 班次详情按排班记录里的 `shop_id + shift_code` 查询，这样即使员工资料没填门店，也能查到 A/B 班时间。
   - 对“明天排班是谁/我明天和谁上班”这类问题，在 system prompt 里明确提供明日班次、明日同班同事和门店班表摘要，避免 AI 瞎猜。

3. 数据完整性补强
   - 补齐当前已排班但 `staff_profiles.shop_id` 为空的用户门店信息，至少让鹿哥和已有排班记录一致。
   - 检查是否存在“有排班但员工资料缺门店”的记录，并通过安全迁移修正。

4. 验证
   - 在预览里打开小精灵抽屉，检查顶部头像、空状态大头像、对话小头像、悬浮按钮不再出现黑块。
   - 用当前账号分别测试：
     - “今天我和谁一起上班？几点到几点？”
     - “明天排班是谁？”
   - 预期回答应能识别鹿哥今天/明天 B 班，并给出同班同事与时间。